import React from "react";
import { SectionContainer } from "@/containers";

export const Footer = () => {
    return (
        <SectionContainer id="footer">
            <p>test</p>
        </SectionContainer>
    );
};